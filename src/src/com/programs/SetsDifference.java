package com.programs;
/*
public class SetsDifference {
import java.io.*;
import java.util.*;

// a = ["a1", "b1", "c1"]
// b = ["b1", d1"]

// result = ["a1", "c1"]


    List<String> findDifference(List<String> setA, List<String> setB) {

        Collectios.sort(setA);
        Collection.sort(SetB);

        int lengthA = setA.size();
        int lengthB = setB.size();

        int index1 = 0;
        int index2 = 0;
        Set<Integer> indexesToBeRemoved = new HashSet();
        while (index1 < lengthA && index2 < lengthB) {

            int result = compare(setA.get(index1), setB.get(index2));

            if (result == 0) {
                // element to be removed from setA
                indexesToBeRemoved.add(index);
            }
            if (result == -1) {
                index1++;
            }
            if (result == 1) {
                index2++;
            }
        }

        for (Integer i; i < indexesToBeRemoved.size(); i++) {
            setA.remove(i);
        }
        return setA;
    }

    //-1 (s1 < s2) , 0 (s1 == s2) , 1 (s1 > s2)
    int compare(String s1, String s2) {

    }

}

*/
